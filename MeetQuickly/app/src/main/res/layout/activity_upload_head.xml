<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.login.UploadHeadActivity">

    <ImageView
        android:id="@+id/iv_back"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:src="@mipmap/ic_back_black"
        android:padding="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        />
    <ImageView
        android:id="@+id/iv_update_head"
        android:layout_width="300dp"
        android:layout_height="300dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="80dp"
        android:src="@mipmap/ic_upload_head"
        />
    <LinearLayout
        android:id="@+id/ll_update_head"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@drawable/shape_bg_bottom"
        app:layout_constraintTop_toBottomOf="@id/iv_update_head"
        app:layout_constraintLeft_toLeftOf="@id/iv_update_head"
        >
        <TextView
            android:id="@+id/tv_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="您好：某某某"
            android:textSize="16dp"
            android:textColor="@color/font_black"
            android:padding="10dp"
            />
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="选择自己最满意的一张照片，带有清晰脸部照片，可 以提高交友成功率哦！"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingBottom="20dp"
            />

    </LinearLayout>

    <TextView
        android:id="@+id/tv_sure"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_margin="38dp"
        android:background="@drawable/shape_bt_gray"
        android:gravity="center"
        android:text="确认"
        android:textColor="@color/font_black"
        android:textSize="18dp"
        app:layout_constraintTop_toBottomOf="@id/ll_update_head" />

</android.support.constraint.ConstraintLayout>